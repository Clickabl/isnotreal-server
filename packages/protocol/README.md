# Authoritative public contract

This repository owns the public wire contract. Keep it free of imports from server domain models. The extension carries an exact, commit-pinned snapshot and digest; coordinate updates explicitly. This package is private until package distribution is selected. See ../../docs/protocol.md for the route and synchronization design.
